export default function Roi() {
  return (
    <section className="py-[78px]">
      <div className="mx-auto max-w-[1080px] px-6">
        <p className="mb-3 text-xs uppercase tracking-[.12em] text-accent-2">
          Why it pays for itself
        </p>
        <h2 className="mb-3.5 max-w-2xl text-[clamp(1.6rem,3.4vw,2.3rem)] font-extrabold tracking-[-.02em]">
          You bill by the hour. Lost knowledge is lost money.
        </h2>
        <p className="max-w-[660px] text-lg text-muted">
          Every hour your team spends hunting for something you already know —
          or redoing work that already existed — is margin you never get back.
          Close the Loop hands those hours back, and catches the client
          problems before they cost you the account.
        </p>
      </div>
    </section>
  );
}
